[package]
name = "zidecar"
version = "0.8.0"
edition = "2021"
license = "MIT"
description = "Ligerito-powered Zcash light server with real-time header chain proofs"
repository = "https://github.com/rotkonetworks/zcli"
keywords = ["zcash", "ligerito", "light-server", "zk-proofs"]
categories = ["cryptography"]

[[bin]]
name = "zidecar"
path = "src/main.rs"

[dependencies]
# zync core types
zync-core = { version = "0.6.1", path = "../../crates/zync-core" }

# ligerito polynomial commitment scheme
ligerito = { version = "0.6.1", path = "../../crates/ligerito", features = ["transcript-blake2b"] }
ligerito-binary-fields = { version = "0.6.1", path = "../../crates/ligerito-binary-fields" }

# async runtime
tokio = { version = "1", features = ["full"] }
tokio-stream = { version = "0.1", features = ["sync", "net"] }
futures = "0.3"

# gRPC (with web support for browser clients)
tonic = "0.11"
tonic-web = "0.11"
prost = "0.12"
prost-types = "0.12"

# orchard tree — needed to compute the canonical orchard tree root
# from zebrad's frontier hex (the "anchor" we sign + bind in the proof).
orchard = { version = "0.12", default-features = false }
incrementalmerkletree = { version = "0.8", features = ["legacy-api"] }

# http client for zebrad RPC
reqwest = { version = "0.11", default-features = false, features = ["json", "rustls-tls"] }
serde_json = "1.0"

# storage (nomt for sparse merkle trees)
nomt = { version = "1.0", features = ["serde"] }
sled = "0.34"  # for simple proof cache

# crypto
blake2 = "0.10"
sha2 = "0.10"
ark-vrf = { version = "0.2", features = ["bandersnatch", "ring"] }
ark-serialize = { version = "0.5", features = ["derive"] }
hkdf = "0.12"

# tower middleware
tower = { version = "0.4", features = ["retry", "timeout", "util", "buffer", "limit"] }
tower-http = { version = "0.4", features = ["trace"] }

# frost relay
rand = "0.8"
async-stream = "0.3"

# utilities
thiserror = "1.0"
anyhow = "1.0"
hex = "0.4"
ed25519-consensus = "2"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
clap = { version = "4", features = ["derive", "env"] }
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"

[build-dependencies]
tonic-build = "0.11"

[dev-dependencies]
tempfile = "3"
